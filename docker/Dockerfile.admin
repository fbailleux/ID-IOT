FROM python:3.11-slim

WORKDIR /app

COPY requirements-admin.txt .
RUN pip install --no-cache-dir -r requirements-admin.txt

COPY admin_service/ ./admin_service/

EXPOSE 8080

CMD ["uvicorn", "admin_service.main:app", "--host", "0.0.0.0", "--port", "8080"]
